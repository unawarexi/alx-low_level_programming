#include <stdio.h>

/**
 * main - "Entry point
 * prints all single digit numbers of base 10 starting from 0 ".
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + 'a');
			c++;
		}
	putchar('\n');
	return (0);
}
